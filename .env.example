# Vision AI Configuration

# Option 1: Use Service Account JSON file (Recommended for production)
# Download the service account JSON from Google Cloud Console
# GOOGLE_VISION_KEY_FILE=./config/google-vision-service-account.json

# Option 2: Use API Key (Simpler for development)
# Get your API key from Google Cloud Console
GOOGLE_VISION_API_KEY=your_vision_ai_api_key_here

# Database Configuration (if not already set)
# DATABASE_URL=postgresql://username:password@localhost:5432/tinder_core

# Other environment variables...
